package ui.model;

import org.openqa.selenium.WebDriver;
import ui.model.base.BasePage;

public class OuterPage extends BasePage {
    protected OuterPage(WebDriver driver) {
        super(driver);
    }
}
